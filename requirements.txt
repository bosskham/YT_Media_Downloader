PySide6>=6.6.0
yt-dlp>=2024.1.1
mutagen>=1.47.0
pyinstaller==6.14.2

# Optional — YouTube Music official lyrics (LyricFind, timed when available)
#   pip install ytmusicapi
ytmusicapi>=1.7.0

# Optional — multi-provider synced lyrics (NetEase, Musixmatch, Lyricsify, Genius, …)
#   pip install syncedlyrics
syncedlyrics>=1.0.0

# Optional — non-Latin lyrics romanization (Japanese → romaji, Chinese → pinyin, etc.)
#   pip install pykakasi pypinyin unidecode
pykakasi>=2.2.1
pypinyin>=0.49.0
unidecode>=1.3.7
